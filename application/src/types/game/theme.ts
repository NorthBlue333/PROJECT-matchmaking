export type Theme =
  | { background: string; color: string }
  | { background: string; colorClass: string }
  | { backgroundClass: string; color: string }
  | { backgroundClass: string; colorClass: string }
